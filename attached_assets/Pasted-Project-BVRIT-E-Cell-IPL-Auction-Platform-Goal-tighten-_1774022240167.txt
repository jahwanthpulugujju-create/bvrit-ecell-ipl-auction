Project: BVRIT E-Cell IPL Auction Platform
Goal: tighten privacy, remove strategic leaks, simplify the auction UI, and preserve only the controls and data the organizer/admin should see.

Implement the following changes exactly.

1. Hide the player pool from all teams before auction
   1.1. Team users must NOT be able to open or browse the full player pool.
   1.2. Remove any public access to the player list from team-facing pages.
   1.3. The full player pool must be visible only to the admin/organizer.
   1.4. Team users should only see the current live auction player when the admin chooses to reveal it.
   1.5. Do not show “upcoming players” anywhere in any team-facing view.
   1.6. Do not show the full roster list in the team dashboard.

2. Make each team fully isolated and private
   2.1. Team A must not be able to open Team B’s page, notes, squad, budget, or any hidden state.
   2.2. Team users should only access their own team after entering the correct team name and password.
   2.3. Add authentication so that a team login session is tied to exactly one team.
   2.4. After login, the user should only see that team’s data and no other team’s data.
   2.5. Prevent direct URL tampering or manual switching to another team slug.
   2.6. If a user tries to access another team, redirect them to the login screen or show “Access denied”.

3. Remove all publicly visible team summary information
   3.1. In the Teams section, do not show purse values, player counts, squad counts, or internal team stats in the public list.
   3.2. The team cards should only show the team name and a generic locked/private state.
   3.3. A user clicking a team should be forced to authenticate before any team details load.
   3.4. After authentication, show only that team’s private dashboard.
   3.5. Do not expose purse or player counts in the home page, teams page, or any open preview card.

4. Remove strategic leakage from the projector/display page
   4.1. The projector display must NOT show upcoming players.
   4.2. The projector display must NOT show team purse values.
   4.3. The projector display must NOT show a leaderboard with team balances.
   4.4. The projector display should only show:
        - current player being auctioned
        - current bid
        - auction timer
        - player statistics
        - sold / unsold status
        - auction ticker for sold players
   4.5. Only the admin panel should show upcoming players and internal scheduling.
   4.6. The projector view should be a clean public broadcast screen with no strategic information.

5. Simplify the auction button layout
   5.1. Remove the extra three-button bidding controls if they create clutter or expose too much.
   5.2. Keep only the minimal set of auction controls required for the admin to run the auction smoothly.
   5.3. Replace multiple small buttons with one clear primary action or a compact controlled bidding interface.
   5.4. The UI should feel simple, clean, and easy for the auctioneer to use live.
   5.5. Do not overload the screen with too many action buttons.

6. Remove player face images from the auction/admin control sections
   6.1. Do not show player face photos in the admin auction control panel.
   6.2. Do not show player face photos in the bidding control panel if they make the UI noisy.
   6.3. Use player name, role, and stats instead of face thumbnails in control-heavy views.
   6.4. If needed, use a small text badge or initials only.
   6.5. Keep photos only where they add value and do not reveal unnecessary visual clutter.
   6.6. The focus should be on auction state, not profile photos in admin screens.

7. Keep player statistics visible where appropriate
   7.1. In the projector/display view, show the current player’s statistics clearly.
   7.2. Show useful stats such as role, batting/bowling summary, and rating.
   7.3. The stats should be readable from a distance on the projector.
   7.4. Keep the stats clean and neatly formatted.
   7.5. Do not show sensitive future auction information in the stats area.

8. Make the pause button work correctly
   8.1. The auction pause button must fully pause the auction timer and bidding state.
   8.2. When paused, no new bids should be accepted.
   8.3. The display should clearly show that the auction is paused.
   8.4. Resuming should continue from the exact paused state.
   8.5. The pause state must persist correctly across refresh if the app already stores state.
   8.6. Add clear admin-only controls for pause and resume.

9. Enforce the player order from the Excel sheet
   9.1. The auction must follow the exact order I provide in the Excel sheet.
   9.2. The order should be:
        - Batsmen first
        - Bowlers second
        - All-rounders third
        - Wicket-keepers last
   9.3. Do not auto-sort by rating, name, or any other field unless explicitly instructed.
   9.4. Preserve the order exactly as supplied in the sheet.
   9.5. The import logic must respect this sequence when building the auction queue.
   9.6. Add a visible admin-only queue view showing the current order.
   9.7. The public display should only show the current player, not the next players.

10. Admin-only controls and visibility
   10.1. The admin panel must remain the only place where upcoming players can be seen.
   10.2. The admin panel should be the only place where team creation, team passwords, player queue management, and auction sequencing are controlled.
   10.3. The admin should be able to start, pause, resume, sell, mark unsold, and move to the next player.
   10.4. Team users must not see admin-only controls.
   10.5. Keep admin controls secure behind password authentication.

11. Update the routing and access logic
   11.1. Public routes must not leak private team data.
   11.2. Team routes must require team login.
   11.3. Admin routes must require admin password.
   11.4. Display routes must be public but only show safe broadcast data.
   11.5. Any route that reveals hidden data must be restricted.

12. Update the UI copy and labels
   12.1. Remove labels that hint at hidden upcoming players.
   12.2. Remove public labels like team purse, remaining budget, or total players from open pages.
   12.3. Replace them with neutral labels such as “Private team dashboard” or “Locked team view”.
   12.4. Keep the interface professional and minimal.

13. Technical implementation requirements
   13.1. Update all components that currently render:
        - player pool
        - team cards
        - leaderboard
        - upcoming player list
        - visible purse totals
        - player face thumbnails in admin controls
   13.2. Move protected data into admin-only or team-auth-only components.
   13.3. Use conditional rendering based on role: admin, team user, or public viewer.
   13.4. Ensure refresh does not leak private data.
   13.5. Ensure localStorage or backend state does not expose hidden lists in the browser to unauthorized users.
   13.6. If data is fetched from an API, filter it server-side before sending to the client.

14. Acceptance criteria
   14.1. A normal visitor cannot see the full player pool.
   14.2. A normal visitor cannot see team purse or player counts.
   14.3. A team user can only access their own team after login.
   14.4. A team user cannot inspect another team’s data.
   14.5. The projector display does not show upcoming players or team balances.
   14.6. The admin can still run the auction smoothly.
   14.7. The pause button works.
   14.8. The player queue follows the exact Excel order: batsmen, then bowlers, then all-rounders, then wicket-keepers.
   14.9. The auction screen is cleaner and less cluttered.
   14.10. No strategic leak is visible to teams before or during the auction.

15. Deliverables
   15.1. Updated privacy-safe team pages.
   15.2. Updated projector display.
   15.3. Updated admin panel.
   15.4. Updated auction queue order logic.
   15.5. Working team login and admin login.
   15.6. Verified pause/resume behavior.
   15.7. Verified hidden upcoming player logic.
   15.8. Verified removal of public purse/team count leakage.

Important:
Do not add RTM or retention in this task.
Do not expose any hidden players or private team data.
Do not redesign the whole site unless needed for these fixes.
Focus only on privacy, access control, auction order, pause behavior, and display simplification.